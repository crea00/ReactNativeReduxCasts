import React from 'react';
import { Text, AppRegistry } from 'react-native';

export default (App = () => <Text>Some Text</Text>);

// Only the 'root' component uses 'AppRegistry'
// AppRegistry.registerComponent('albums', () => App);
